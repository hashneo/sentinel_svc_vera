{
	"containers": [
		{
			"name": "c1",
			"image": "docker-sandbox.concurtech.net/cat/vera:latest",
			"port": 5000,
			"env": []
		}
	],
	"name": "vera",
	"targetPort": 5000,
	"locations": [
		{
			"name": "msp"
		}
	],
    "clusters": 2, 
    "replicas": 2,
	"user": "${KUBE_USER}",
	"token": "${KUBE_TOKEN}",
	"namespace": "cat",
	"loadBalanced": true,
	"autodeploy": true,
	"deploymentStrategy": "fast",
	"healthCheck": "tcp",
	"owner": "cat"
}
